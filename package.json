{
  "name": "acp-to-agui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "install:backend": "uv pip install -e ./backend",
    "dev:backend": "node scripts/dev-backend.mjs",
    "postinstall": "uv pip install -e ./backend"
  },
  "devDependencies": {
    "@agentclientprotocol/claude-agent-acp": "^0.36.1",
    "@zed-industries/codex-acp": "*"
  }
}
